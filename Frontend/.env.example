# Copy to .env.local and set your values. Restart dev server after changes.
# 
# ────────────────────────────────────────────────
# Supabase Configuration
# ────────────────────────────────────────────────
# Get these from your Supabase project settings:
# https://app.supabase.com/project/_/settings/api
#
# Public (safe for client-side / browser)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Server-only – keep secret! (API routes, Server Actions, server components)
# Only use this in server-side code, never expose to the client!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ────────────────────────────────────────────────
# Application Configuration
# ────────────────────────────────────────────────
# Site URL (important for auth redirects, especially OAuth)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# When you deploy → change to: https://your-app-name.vercel.app

# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000
